# JWKS volume placeholder

This folder is used by the root `Makefile` as a mount volumem where a `keys.jwks` file should be placed in order to test Bearer Authentication on Proxy CONNECT requests.


An example JWKS can be generated with this site: `https://jwkset.com/generate`.

